



#include "dplib/solution.hpp"

auto dp::solution::isSubsequence(std::string s, std::string t) -> bool {
        // int n = s.length();
        // int m = t.length();
        // int i = 0;
        // int j = 0;
        // while (i < n && j < m) {
        //     if (s[i] == t[j]) {
        //         i++;
        //     }
        //     j++;
        // }
        // return i == n;
        // auto dp = std::array<std::array<int, 5>, 5>();

        return true;
    }